import type {OptimizationResponse,PortfolioInput,PortfolioResponse,RiskResponse} from "./types";
const API_BASE=process.env.NEXT_PUBLIC_API_BASE_URL??"http://localhost:8000";
export class ApiError extends Error{constructor(message:string,public status?:number){super(message);this.name="ApiError"}}
async function post<T>(path:string,body:unknown):Promise<T>{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),60000);try{const response=await fetch(`${API_BASE}${path}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body),signal:controller.signal});let payload:unknown;try{payload=await response.json()}catch{throw new ApiError("The analytics service returned an unreadable response.",response.status)}if(!response.ok){const detail=typeof payload==="object"&&payload!==null&&"detail" in payload?(payload as {detail:unknown}).detail:null;throw new ApiError(typeof detail==="string"?detail:"The portfolio inputs were rejected by the analytics service.",response.status)}if(typeof payload!=="object"||payload===null)throw new ApiError("The analytics service returned an invalid response.");return payload as T}catch(error){if(error instanceof ApiError)throw error;if(error instanceof DOMException&&error.name==="AbortError")throw new ApiError("The analytics request timed out. Please try again.");throw new ApiError("PortfolioIQ could not reach the analytics service.")}finally{clearTimeout(timer)}}
export const analyzePortfolio=(input:PortfolioInput)=>post<PortfolioResponse>("/api/analytics/portfolio",input);
export const analyzeRisk=(input:PortfolioInput)=>post<RiskResponse>("/api/analytics/portfolio/risk",{...input,confidence_levels:[.95,.99]});
export const optimizePortfolio=(input:PortfolioInput,custom?:Record<string,number>)=>post<OptimizationResponse>("/api/analytics/portfolio/optimize",{...input,objective:"maximum_sharpe",minimum_asset_weight:0,maximum_asset_weight:1,frontier_point_count:30,requested_strategies:["equal_weight","minimum_variance","maximum_sharpe","risk_parity","efficient_frontier"],hypothetical_scenarios:["Equity Selloff","Rate Shock","Risk-Off","Inflation Shock"],custom_asset_shocks:custom});
export const getHealth=async()=>{const response=await fetch(`${API_BASE}/health`,{cache:"no-store"});if(!response.ok)throw new ApiError("Backend unavailable",response.status);return response.json() as Promise<{status:string;release:string}>};
