"use client";
import {DrawdownChart,GrowthChart,PeriodReturnsChart} from "@/components/charts/PortfolioCharts";import {MetricCard} from "@/components/common/MetricCard";import {PageHeader} from "@/components/common/PageHeader";import {AnalysisGate} from "@/components/common/States";import {usePortfolio} from "@/context/PortfolioContext";import {formatDate,formatPercent,formatRatio,titleCase} from "@/lib/formatters";
export default function PerformancePage(){const {analysis}=usePortfolio();return <><PageHeader title="Performance" description="Absolute and benchmark-relative results from aligned backend return calculations."/><AnalysisGate>{analysis&&<><section className="metric-grid cols-3">{[["Total Return",formatPercent(analysis.portfolio.performance.total_return)],["CAGR",formatPercent(analysis.portfolio.performance.cagr)],["Annualized Volatility",formatPercent(analysis.portfolio.performance.annualized_volatility)],["Sharpe",formatRatio(analysis.portfolio.performance.sharpe_ratio)],["Sortino",formatRatio(analysis.portfolio.performance.sortino_ratio)],["Calmar",formatRatio(analysis.portfolio.performance.calmar_ratio)]].map(([label,value])=><MetricCard key={label} label={label} value={value}/>)}</section><section className="chart-grid"><div className="panel chart-panel span-2"><h2>Portfolio vs benchmark</h2><GrowthChart data={analysis.portfolio.cumulative_growth} benchmark={analysis.portfolio.benchmark_ticker}/></div><div className="panel chart-panel"><h2>Drawdown history</h2><DrawdownChart data={analysis.portfolio.drawdown_series}/></div><div className="panel chart-panel"><h2>Monthly returns</h2><PeriodReturnsChart rows={analysis.portfolio.monthly_returns}/></div><div className="panel chart-panel"><h2>Annual returns</h2><PeriodReturnsChart rows={analysis.portfolio.annual_returns} annual/></div></section><section className="panel"><div className="section-heading"><div><h2>Benchmark-relative metrics</h2><p>Portfolio compared with {analysis.portfolio.benchmark_ticker} over shared dates.</p></div></div><div className="metric-grid compact cols-4">{Object.entries(analysis.portfolio.benchmark).map(([key,value])=><MetricCard key={key} label={titleCase(key)} value={key==="beta"||key==="r_squared"||key.includes("ratio")?formatRatio(value):formatPercent(value)}/>)}</div></section><section className="panel"><h2>Best and worst periods</h2><div className="table-wrap"><table><thead><tr><th>Period</th><th>Date</th><th>Return</th></tr></thead><tbody>{Object.entries(analysis.portfolio.best_worst_periods).map(([name,row])=><tr key={name}><td>{titleCase(name)}</td><td>{formatDate(row.date)}</td><td className={(row.value??0)>=0?"positive-cell":"negative-cell"}>{formatPercent(row.value)}</td></tr>)}</tbody></table></div></section></>}</AnalysisGate></>}
